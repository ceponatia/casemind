{
    "name": "@casemind/auth",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "exports": {
        ".": "./src/index.ts"
    },
    "dependencies": {
        "@casemind/rbac": "workspace:*",
        "redis": "^5.8.2"
    },
    "devDependencies": {
        "@casemind/test-utils": "workspace:*",
        "fast-check": "^4.0.1",
        "vitest": "^3.0.8"
    },
    "scripts": {
        "lint": "eslint src tests",
        "typecheck": "tsc -p tsconfig.json",
        "test": "vitest run --config vitest.config.ts --dir tests/unit",
        "test:integration": "vitest run --config vitest.config.ts --dir tests/integration"
    }
}